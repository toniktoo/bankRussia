import { createAction } from "redux-actions";

export const actionSetInitDepositData = createAction("SET_INIT_DEPOSIT_DATA");
export const actionSetActiveTypeDeposit = createAction(
  "SET_ACTIVE_TYPE_DEPOSIT"
);
export const actionSetActiveDeposit = createAction("SET_ACTIVE_DEPOSIT");
